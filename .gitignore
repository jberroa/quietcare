# Dependencies
node_modules/

# Build output
dist/
build/
*.tsbuildinfo

# Vite
.vite/

# Env — never commit secrets (examples are tracked)
.env
.env.*
!.env.example
!**/.env.example

# SQLite (local + api; Coolify uses a volume, not git)
**/data/*.db
**/data/*.db-wal
**/data/*.db-shm

# Tests / coverage
coverage/

# Logs & OS
*.log
.DS_Store

# Editor / IDE (optional local settings)
.idea/
*.swp
*.swo
